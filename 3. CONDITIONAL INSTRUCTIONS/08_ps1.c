#include <stdio.h>

int main()
{
    int a = 10;
    if (a = 11)
        printf("I am 11"); //Here = is assignment and 11 is assigned to "a" which is non zero value and is True so the code is executed
    else
        printf("I am not 11"); 
    return 0;
}